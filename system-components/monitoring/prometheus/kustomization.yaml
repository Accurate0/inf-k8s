# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./kanidmoauth2client.yaml
  - ./securitypolicy.yaml
configMapGenerator:
  - name: prometheus-kanidm-icon
    files:
      - prometheus.svg
generatorOptions:
  disableNameSuffixHash: true
