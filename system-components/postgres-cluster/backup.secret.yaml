apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cloudnative-pg-cluster-backup-s3-creds
  namespace: cnpg-system
spec:
  refreshInterval: 900s
  secretStoreRef:
    name: infisical-cluster-backup-secret
    kind: ClusterSecretStore
  target:
    name: cloudnative-pg-cluster-backup-s3-creds
    creationPolicy: Owner
    template:
      type: Opaque
  dataFrom:
    - find:
        name:
          regexp: ".*"
