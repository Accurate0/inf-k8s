{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Pin GitHub Actions to immutable SHA for security.",
  "packageRules": [
    {
      "matchManagers": ["github-actions"],
      "pinDigests": true,
      "groupName": "github-actions",
      "description": "Pin GitHub Actions to immutable SHA for security and group updates into one PR.",
      "matchDepNames": ["!/^helm$/"]
    }
  ]
}
