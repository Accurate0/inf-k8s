{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Wait before adopting new npm releases to avoid supply-chain attacks.",
  "packageRules": [
    {
      "matchManagers": ["npm"],
      "minimumReleaseAge": "14 days",
      "internalChecksFilter": "strict",
      "rebaseWhen": "behind-base-branch",
      "schedule": ["after 2am and before 3am"],
      "description": "Hold npm updates until releases have aged 14 days; rebase stale PRs once daily."
    }
  ]
}
