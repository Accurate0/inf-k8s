apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: litellm-managed-secrets
  namespace: litellm
spec:
  refreshInterval: 900s
  secretStoreRef:
    name: infisical-litellm-secret
    kind: SecretStore
  target:
    name: litellm-managed-secrets
    creationPolicy: Owner
    template:
      type: Opaque
  dataFrom:
    - find:
        conversionStrategy: Default
        decodingStrategy: None
        name:
          regexp: ".*"
        nullBytePolicy: Ignore
