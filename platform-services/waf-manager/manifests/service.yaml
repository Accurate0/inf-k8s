# yaml-language-server: $schema=https://k8s-schemas.anurag.sh/core/service_v1.json
apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: waf-manager
  labels:
    app: waf-manager
spec:
  selector:
    app: waf-manager
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
