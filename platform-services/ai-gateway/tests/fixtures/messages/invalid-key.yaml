endpoint: /v1/messages
provider:
  dialect: anthropic
  models:
    - claude-fable-5
key:
  auth: invalid
request:
  model: claude-fable-5
  max_tokens: 64
  messages:
    - role: user
      content: hello
