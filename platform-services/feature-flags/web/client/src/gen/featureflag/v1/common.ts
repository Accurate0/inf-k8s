// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v3.19.1
// source: featureflag/v1/common.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Struct, Value } from "../../google/protobuf/struct";

export const protobufPackage = "featureflag.v1";

/** The typed kind of a flag's variant values. A flag's variants all share its type. */
export enum ValueType {
  VALUE_TYPE_UNSPECIFIED = 0,
  VALUE_TYPE_BOOLEAN = 1,
  VALUE_TYPE_STRING = 2,
  VALUE_TYPE_INTEGER = 3,
  VALUE_TYPE_FLOAT = 4,
  VALUE_TYPE_OBJECT = 5,
  UNRECOGNIZED = -1,
}

/** Why a particular value was returned, mapped to OpenFeature reasons by the provider. */
export enum Reason {
  REASON_UNSPECIFIED = 0,
  REASON_STATIC = 1,
  REASON_DEFAULT = 2,
  REASON_TARGETING_MATCH = 3,
  REASON_SPLIT = 4,
  REASON_DISABLED = 5,
  REASON_ERROR = 6,
  UNRECOGNIZED = -1,
}

/** Operators a segment constraint can apply to a context attribute. */
export enum ConstraintOperator {
  CONSTRAINT_OPERATOR_UNSPECIFIED = 0,
  CONSTRAINT_OPERATOR_EQ = 1,
  CONSTRAINT_OPERATOR_NEQ = 2,
  CONSTRAINT_OPERATOR_IN = 3,
  CONSTRAINT_OPERATOR_NOT_IN = 4,
  CONSTRAINT_OPERATOR_CONTAINS = 5,
  CONSTRAINT_OPERATOR_STARTS_WITH = 6,
  CONSTRAINT_OPERATOR_ENDS_WITH = 7,
  CONSTRAINT_OPERATOR_GT = 8,
  CONSTRAINT_OPERATOR_GTE = 9,
  CONSTRAINT_OPERATOR_LT = 10,
  CONSTRAINT_OPERATOR_LTE = 11,
  CONSTRAINT_OPERATOR_EXISTS = 12,
  CONSTRAINT_OPERATOR_REGEX = 13,
  UNRECOGNIZED = -1,
}

/** The OpenFeature-style evaluation context the engine matches rules against. */
export interface EvaluationContext {
  /** The key used for stable percentage-rollout bucketing. */
  targetingKey: string;
  /** Arbitrary attributes (string/number/bool) referenced by segment constraints. */
  attributes?: { [key: string]: any } | undefined;
}

export interface Variant {
  key: string;
  /**
   * The variant payload. Scalar types use the matching google.protobuf.Value kind;
   * object flags carry a struct.
   */
  value?: any | undefined;
}

export interface Constraint {
  attribute: string;
  operator: ConstraintOperator;
  /** Comparison operands; most operators use a single value, IN/NOT_IN use the list. */
  values: any[];
}

export interface Segment {
  key: string;
  name: string;
  /** A context matches the segment only when every constraint matches (AND). */
  constraints: Constraint[];
}

/** One weighted variant within a rule's percentage split. */
export interface Distribution {
  variantKey: string;
  /** Integer percentage [0,100]; weights within a rule must sum to 100. */
  weight: number;
}

/**
 * An ordered targeting rule on a flag. When the segment matches, the rule serves
 * either a single variant or a weighted split of variants.
 */
export interface Rule {
  rank: number;
  segmentKey: string;
  variantKey: string;
  distributions: Distribution[];
}

export interface Flag {
  key: string;
  valueType: ValueType;
  enabled: boolean;
  defaultVariantKey: string;
  archived: boolean;
  variants: Variant[];
  rules: Rule[];
}

function createBaseEvaluationContext(): EvaluationContext {
  return { targetingKey: "", attributes: undefined };
}

export const EvaluationContext: MessageFns<EvaluationContext> = {
  encode(message: EvaluationContext, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.targetingKey !== "") {
      writer.uint32(10).string(message.targetingKey);
    }
    if (message.attributes !== undefined) {
      Struct.encode(Struct.wrap(message.attributes), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EvaluationContext {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEvaluationContext();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.targetingKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.attributes = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<EvaluationContext>, I>>(base?: I): EvaluationContext {
    return EvaluationContext.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EvaluationContext>, I>>(object: I): EvaluationContext {
    const message = createBaseEvaluationContext();
    message.targetingKey = object.targetingKey ?? "";
    message.attributes = object.attributes ?? undefined;
    return message;
  },
};

function createBaseVariant(): Variant {
  return { key: "", value: undefined };
}

export const Variant: MessageFns<Variant> = {
  encode(message: Variant, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      Value.encode(Value.wrap(message.value), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Variant {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVariant();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = Value.unwrap(Value.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Variant>, I>>(base?: I): Variant {
    return Variant.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Variant>, I>>(object: I): Variant {
    const message = createBaseVariant();
    message.key = object.key ?? "";
    message.value = object.value ?? undefined;
    return message;
  },
};

function createBaseConstraint(): Constraint {
  return { attribute: "", operator: 0, values: [] };
}

export const Constraint: MessageFns<Constraint> = {
  encode(message: Constraint, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.attribute !== "") {
      writer.uint32(10).string(message.attribute);
    }
    if (message.operator !== 0) {
      writer.uint32(16).int32(message.operator);
    }
    for (const v of message.values) {
      Value.encode(Value.wrap(v!), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Constraint {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConstraint();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.attribute = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.operator = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.values.push(Value.unwrap(Value.decode(reader, reader.uint32())));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Constraint>, I>>(base?: I): Constraint {
    return Constraint.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Constraint>, I>>(object: I): Constraint {
    const message = createBaseConstraint();
    message.attribute = object.attribute ?? "";
    message.operator = object.operator ?? 0;
    message.values = object.values?.map((e) => e) || [];
    return message;
  },
};

function createBaseSegment(): Segment {
  return { key: "", name: "", constraints: [] };
}

export const Segment: MessageFns<Segment> = {
  encode(message: Segment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    for (const v of message.constraints) {
      Constraint.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Segment {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSegment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.constraints.push(Constraint.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Segment>, I>>(base?: I): Segment {
    return Segment.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Segment>, I>>(object: I): Segment {
    const message = createBaseSegment();
    message.key = object.key ?? "";
    message.name = object.name ?? "";
    message.constraints = object.constraints?.map((e) => Constraint.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDistribution(): Distribution {
  return { variantKey: "", weight: 0 };
}

export const Distribution: MessageFns<Distribution> = {
  encode(message: Distribution, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.variantKey !== "") {
      writer.uint32(10).string(message.variantKey);
    }
    if (message.weight !== 0) {
      writer.uint32(16).uint32(message.weight);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Distribution {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDistribution();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.variantKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.weight = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Distribution>, I>>(base?: I): Distribution {
    return Distribution.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Distribution>, I>>(object: I): Distribution {
    const message = createBaseDistribution();
    message.variantKey = object.variantKey ?? "";
    message.weight = object.weight ?? 0;
    return message;
  },
};

function createBaseRule(): Rule {
  return { rank: 0, segmentKey: "", variantKey: "", distributions: [] };
}

export const Rule: MessageFns<Rule> = {
  encode(message: Rule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rank !== 0) {
      writer.uint32(8).uint32(message.rank);
    }
    if (message.segmentKey !== "") {
      writer.uint32(18).string(message.segmentKey);
    }
    if (message.variantKey !== "") {
      writer.uint32(26).string(message.variantKey);
    }
    for (const v of message.distributions) {
      Distribution.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Rule {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRule();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.rank = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.segmentKey = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.variantKey = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.distributions.push(Distribution.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Rule>, I>>(base?: I): Rule {
    return Rule.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Rule>, I>>(object: I): Rule {
    const message = createBaseRule();
    message.rank = object.rank ?? 0;
    message.segmentKey = object.segmentKey ?? "";
    message.variantKey = object.variantKey ?? "";
    message.distributions = object.distributions?.map((e) => Distribution.fromPartial(e)) || [];
    return message;
  },
};

function createBaseFlag(): Flag {
  return { key: "", valueType: 0, enabled: false, defaultVariantKey: "", archived: false, variants: [], rules: [] };
}

export const Flag: MessageFns<Flag> = {
  encode(message: Flag, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.valueType !== 0) {
      writer.uint32(16).int32(message.valueType);
    }
    if (message.enabled !== false) {
      writer.uint32(24).bool(message.enabled);
    }
    if (message.defaultVariantKey !== "") {
      writer.uint32(34).string(message.defaultVariantKey);
    }
    if (message.archived !== false) {
      writer.uint32(40).bool(message.archived);
    }
    for (const v of message.variants) {
      Variant.encode(v!, writer.uint32(50).fork()).join();
    }
    for (const v of message.rules) {
      Rule.encode(v!, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Flag {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFlag();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.valueType = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.defaultVariantKey = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.archived = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.variants.push(Variant.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.rules.push(Rule.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Flag>, I>>(base?: I): Flag {
    return Flag.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Flag>, I>>(object: I): Flag {
    const message = createBaseFlag();
    message.key = object.key ?? "";
    message.valueType = object.valueType ?? 0;
    message.enabled = object.enabled ?? false;
    message.defaultVariantKey = object.defaultVariantKey ?? "";
    message.archived = object.archived ?? false;
    message.variants = object.variants?.map((e) => Variant.fromPartial(e)) || [];
    message.rules = object.rules?.map((e) => Rule.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
