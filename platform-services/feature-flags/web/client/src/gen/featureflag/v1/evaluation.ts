// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v3.19.1
// source: featureflag/v1/evaluation.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientReadableStream,
  type ClientUnaryCall,
  type handleServerStreamingCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { Struct, Value } from "../../google/protobuf/struct";
import { EvaluationContext, Flag, Reason, Segment, ValueType } from "./common";

export const protobufPackage = "featureflag.v1";

export enum EventType {
  EVENT_TYPE_UNSPECIFIED = 0,
  EVENT_TYPE_READY = 1,
  EVENT_TYPE_CONFIGURATION_CHANGED = 2,
  UNRECOGNIZED = -1,
}

export interface GetSnapshotRequest {
}

export interface SnapshotResponse {
  version: number;
  flags: Flag[];
  segments: Segment[];
}

export interface ResolveRequest {
  flagKey: string;
  context?: EvaluationContext | undefined;
}

/** Common resolution metadata shared by every typed response. */
export interface ResolutionMeta {
  variant: string;
  reason: Reason;
  /**
   * Set only when reason is REASON_ERROR; an OpenFeature error code such as
   * FLAG_NOT_FOUND, TYPE_MISMATCH, PARSE_ERROR.
   */
  errorCode: string;
  errorMessage: string;
}

export interface ResolveBooleanResponse {
  value: boolean;
  meta?: ResolutionMeta | undefined;
}

export interface ResolveStringResponse {
  value: string;
  meta?: ResolutionMeta | undefined;
}

export interface ResolveIntegerResponse {
  value: number;
  meta?: ResolutionMeta | undefined;
}

export interface ResolveFloatResponse {
  value: number;
  meta?: ResolutionMeta | undefined;
}

export interface ResolveObjectResponse {
  value?: { [key: string]: any } | undefined;
  meta?: ResolutionMeta | undefined;
}

export interface ResolveAllRequest {
  context?: EvaluationContext | undefined;
}

export interface EvaluatedFlag {
  flagKey: string;
  valueType: ValueType;
  value?: any | undefined;
  meta?: ResolutionMeta | undefined;
}

export interface ResolveAllResponse {
  flags: EvaluatedFlag[];
}

export interface EventStreamRequest {
}

export interface Event {
  type: EventType;
  /** Global monotonically increasing config version after the change. */
  configVersion: number;
  /** Flag keys affected by a CONFIGURATION_CHANGED event (empty for READY). */
  changedFlagKeys: string[];
}

function createBaseGetSnapshotRequest(): GetSnapshotRequest {
  return {};
}

export const GetSnapshotRequest: MessageFns<GetSnapshotRequest> = {
  encode(_: GetSnapshotRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSnapshotRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSnapshotRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<GetSnapshotRequest>, I>>(base?: I): GetSnapshotRequest {
    return GetSnapshotRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSnapshotRequest>, I>>(_: I): GetSnapshotRequest {
    const message = createBaseGetSnapshotRequest();
    return message;
  },
};

function createBaseSnapshotResponse(): SnapshotResponse {
  return { version: 0, flags: [], segments: [] };
}

export const SnapshotResponse: MessageFns<SnapshotResponse> = {
  encode(message: SnapshotResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== 0) {
      writer.uint32(8).int64(message.version);
    }
    for (const v of message.flags) {
      Flag.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.segments) {
      Segment.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SnapshotResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSnapshotResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.version = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.flags.push(Flag.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.segments.push(Segment.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<SnapshotResponse>, I>>(base?: I): SnapshotResponse {
    return SnapshotResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SnapshotResponse>, I>>(object: I): SnapshotResponse {
    const message = createBaseSnapshotResponse();
    message.version = object.version ?? 0;
    message.flags = object.flags?.map((e) => Flag.fromPartial(e)) || [];
    message.segments = object.segments?.map((e) => Segment.fromPartial(e)) || [];
    return message;
  },
};

function createBaseResolveRequest(): ResolveRequest {
  return { flagKey: "", context: undefined };
}

export const ResolveRequest: MessageFns<ResolveRequest> = {
  encode(message: ResolveRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.flagKey !== "") {
      writer.uint32(10).string(message.flagKey);
    }
    if (message.context !== undefined) {
      EvaluationContext.encode(message.context, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResolveRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResolveRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.flagKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.context = EvaluationContext.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ResolveRequest>, I>>(base?: I): ResolveRequest {
    return ResolveRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResolveRequest>, I>>(object: I): ResolveRequest {
    const message = createBaseResolveRequest();
    message.flagKey = object.flagKey ?? "";
    message.context = (object.context !== undefined && object.context !== null)
      ? EvaluationContext.fromPartial(object.context)
      : undefined;
    return message;
  },
};

function createBaseResolutionMeta(): ResolutionMeta {
  return { variant: "", reason: 0, errorCode: "", errorMessage: "" };
}

export const ResolutionMeta: MessageFns<ResolutionMeta> = {
  encode(message: ResolutionMeta, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.variant !== "") {
      writer.uint32(10).string(message.variant);
    }
    if (message.reason !== 0) {
      writer.uint32(16).int32(message.reason);
    }
    if (message.errorCode !== "") {
      writer.uint32(26).string(message.errorCode);
    }
    if (message.errorMessage !== "") {
      writer.uint32(34).string(message.errorMessage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResolutionMeta {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResolutionMeta();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.variant = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.reason = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.errorCode = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ResolutionMeta>, I>>(base?: I): ResolutionMeta {
    return ResolutionMeta.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResolutionMeta>, I>>(object: I): ResolutionMeta {
    const message = createBaseResolutionMeta();
    message.variant = object.variant ?? "";
    message.reason = object.reason ?? 0;
    message.errorCode = object.errorCode ?? "";
    message.errorMessage = object.errorMessage ?? "";
    return message;
  },
};

function createBaseResolveBooleanResponse(): ResolveBooleanResponse {
  return { value: false, meta: undefined };
}

export const ResolveBooleanResponse: MessageFns<ResolveBooleanResponse> = {
  encode(message: ResolveBooleanResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value !== false) {
      writer.uint32(8).bool(message.value);
    }
    if (message.meta !== undefined) {
      ResolutionMeta.encode(message.meta, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResolveBooleanResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResolveBooleanResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.value = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.meta = ResolutionMeta.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ResolveBooleanResponse>, I>>(base?: I): ResolveBooleanResponse {
    return ResolveBooleanResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResolveBooleanResponse>, I>>(object: I): ResolveBooleanResponse {
    const message = createBaseResolveBooleanResponse();
    message.value = object.value ?? false;
    message.meta = (object.meta !== undefined && object.meta !== null)
      ? ResolutionMeta.fromPartial(object.meta)
      : undefined;
    return message;
  },
};

function createBaseResolveStringResponse(): ResolveStringResponse {
  return { value: "", meta: undefined };
}

export const ResolveStringResponse: MessageFns<ResolveStringResponse> = {
  encode(message: ResolveStringResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value !== "") {
      writer.uint32(10).string(message.value);
    }
    if (message.meta !== undefined) {
      ResolutionMeta.encode(message.meta, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResolveStringResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResolveStringResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.value = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.meta = ResolutionMeta.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ResolveStringResponse>, I>>(base?: I): ResolveStringResponse {
    return ResolveStringResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResolveStringResponse>, I>>(object: I): ResolveStringResponse {
    const message = createBaseResolveStringResponse();
    message.value = object.value ?? "";
    message.meta = (object.meta !== undefined && object.meta !== null)
      ? ResolutionMeta.fromPartial(object.meta)
      : undefined;
    return message;
  },
};

function createBaseResolveIntegerResponse(): ResolveIntegerResponse {
  return { value: 0, meta: undefined };
}

export const ResolveIntegerResponse: MessageFns<ResolveIntegerResponse> = {
  encode(message: ResolveIntegerResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value !== 0) {
      writer.uint32(8).int64(message.value);
    }
    if (message.meta !== undefined) {
      ResolutionMeta.encode(message.meta, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResolveIntegerResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResolveIntegerResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.value = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.meta = ResolutionMeta.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ResolveIntegerResponse>, I>>(base?: I): ResolveIntegerResponse {
    return ResolveIntegerResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResolveIntegerResponse>, I>>(object: I): ResolveIntegerResponse {
    const message = createBaseResolveIntegerResponse();
    message.value = object.value ?? 0;
    message.meta = (object.meta !== undefined && object.meta !== null)
      ? ResolutionMeta.fromPartial(object.meta)
      : undefined;
    return message;
  },
};

function createBaseResolveFloatResponse(): ResolveFloatResponse {
  return { value: 0, meta: undefined };
}

export const ResolveFloatResponse: MessageFns<ResolveFloatResponse> = {
  encode(message: ResolveFloatResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value !== 0) {
      writer.uint32(9).double(message.value);
    }
    if (message.meta !== undefined) {
      ResolutionMeta.encode(message.meta, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResolveFloatResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResolveFloatResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.value = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.meta = ResolutionMeta.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ResolveFloatResponse>, I>>(base?: I): ResolveFloatResponse {
    return ResolveFloatResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResolveFloatResponse>, I>>(object: I): ResolveFloatResponse {
    const message = createBaseResolveFloatResponse();
    message.value = object.value ?? 0;
    message.meta = (object.meta !== undefined && object.meta !== null)
      ? ResolutionMeta.fromPartial(object.meta)
      : undefined;
    return message;
  },
};

function createBaseResolveObjectResponse(): ResolveObjectResponse {
  return { value: undefined, meta: undefined };
}

export const ResolveObjectResponse: MessageFns<ResolveObjectResponse> = {
  encode(message: ResolveObjectResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value !== undefined) {
      Struct.encode(Struct.wrap(message.value), writer.uint32(10).fork()).join();
    }
    if (message.meta !== undefined) {
      ResolutionMeta.encode(message.meta, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResolveObjectResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResolveObjectResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.value = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.meta = ResolutionMeta.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ResolveObjectResponse>, I>>(base?: I): ResolveObjectResponse {
    return ResolveObjectResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResolveObjectResponse>, I>>(object: I): ResolveObjectResponse {
    const message = createBaseResolveObjectResponse();
    message.value = object.value ?? undefined;
    message.meta = (object.meta !== undefined && object.meta !== null)
      ? ResolutionMeta.fromPartial(object.meta)
      : undefined;
    return message;
  },
};

function createBaseResolveAllRequest(): ResolveAllRequest {
  return { context: undefined };
}

export const ResolveAllRequest: MessageFns<ResolveAllRequest> = {
  encode(message: ResolveAllRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      EvaluationContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResolveAllRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResolveAllRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = EvaluationContext.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ResolveAllRequest>, I>>(base?: I): ResolveAllRequest {
    return ResolveAllRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResolveAllRequest>, I>>(object: I): ResolveAllRequest {
    const message = createBaseResolveAllRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? EvaluationContext.fromPartial(object.context)
      : undefined;
    return message;
  },
};

function createBaseEvaluatedFlag(): EvaluatedFlag {
  return { flagKey: "", valueType: 0, value: undefined, meta: undefined };
}

export const EvaluatedFlag: MessageFns<EvaluatedFlag> = {
  encode(message: EvaluatedFlag, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.flagKey !== "") {
      writer.uint32(10).string(message.flagKey);
    }
    if (message.valueType !== 0) {
      writer.uint32(16).int32(message.valueType);
    }
    if (message.value !== undefined) {
      Value.encode(Value.wrap(message.value), writer.uint32(26).fork()).join();
    }
    if (message.meta !== undefined) {
      ResolutionMeta.encode(message.meta, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EvaluatedFlag {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEvaluatedFlag();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.flagKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.valueType = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.value = Value.unwrap(Value.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.meta = ResolutionMeta.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<EvaluatedFlag>, I>>(base?: I): EvaluatedFlag {
    return EvaluatedFlag.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EvaluatedFlag>, I>>(object: I): EvaluatedFlag {
    const message = createBaseEvaluatedFlag();
    message.flagKey = object.flagKey ?? "";
    message.valueType = object.valueType ?? 0;
    message.value = object.value ?? undefined;
    message.meta = (object.meta !== undefined && object.meta !== null)
      ? ResolutionMeta.fromPartial(object.meta)
      : undefined;
    return message;
  },
};

function createBaseResolveAllResponse(): ResolveAllResponse {
  return { flags: [] };
}

export const ResolveAllResponse: MessageFns<ResolveAllResponse> = {
  encode(message: ResolveAllResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.flags) {
      EvaluatedFlag.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResolveAllResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResolveAllResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.flags.push(EvaluatedFlag.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ResolveAllResponse>, I>>(base?: I): ResolveAllResponse {
    return ResolveAllResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResolveAllResponse>, I>>(object: I): ResolveAllResponse {
    const message = createBaseResolveAllResponse();
    message.flags = object.flags?.map((e) => EvaluatedFlag.fromPartial(e)) || [];
    return message;
  },
};

function createBaseEventStreamRequest(): EventStreamRequest {
  return {};
}

export const EventStreamRequest: MessageFns<EventStreamRequest> = {
  encode(_: EventStreamRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventStreamRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventStreamRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<EventStreamRequest>, I>>(base?: I): EventStreamRequest {
    return EventStreamRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventStreamRequest>, I>>(_: I): EventStreamRequest {
    const message = createBaseEventStreamRequest();
    return message;
  },
};

function createBaseEvent(): Event {
  return { type: 0, configVersion: 0, changedFlagKeys: [] };
}

export const Event: MessageFns<Event> = {
  encode(message: Event, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    if (message.configVersion !== 0) {
      writer.uint32(16).int64(message.configVersion);
    }
    for (const v of message.changedFlagKeys) {
      writer.uint32(26).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Event {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.configVersion = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.changedFlagKeys.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Event>, I>>(base?: I): Event {
    return Event.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Event>, I>>(object: I): Event {
    const message = createBaseEvent();
    message.type = object.type ?? 0;
    message.configVersion = object.configVersion ?? 0;
    message.changedFlagKeys = object.changedFlagKeys?.map((e) => e) || [];
    return message;
  },
};

/**
 * Read path: resolve flag values for a given evaluation context, and stream
 * configuration-change events so providers can invalidate caches and emit
 * OpenFeature CONFIGURATION_CHANGED.
 */
export type EvaluationService = typeof EvaluationService;
export const EvaluationService = {
  resolveBoolean: {
    path: "/featureflag.v1.Evaluation/ResolveBoolean" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ResolveRequest): Buffer => Buffer.from(ResolveRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ResolveRequest => ResolveRequest.decode(value),
    responseSerialize: (value: ResolveBooleanResponse): Buffer =>
      Buffer.from(ResolveBooleanResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ResolveBooleanResponse => ResolveBooleanResponse.decode(value),
  },
  resolveString: {
    path: "/featureflag.v1.Evaluation/ResolveString" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ResolveRequest): Buffer => Buffer.from(ResolveRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ResolveRequest => ResolveRequest.decode(value),
    responseSerialize: (value: ResolveStringResponse): Buffer =>
      Buffer.from(ResolveStringResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ResolveStringResponse => ResolveStringResponse.decode(value),
  },
  resolveInteger: {
    path: "/featureflag.v1.Evaluation/ResolveInteger" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ResolveRequest): Buffer => Buffer.from(ResolveRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ResolveRequest => ResolveRequest.decode(value),
    responseSerialize: (value: ResolveIntegerResponse): Buffer =>
      Buffer.from(ResolveIntegerResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ResolveIntegerResponse => ResolveIntegerResponse.decode(value),
  },
  resolveFloat: {
    path: "/featureflag.v1.Evaluation/ResolveFloat" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ResolveRequest): Buffer => Buffer.from(ResolveRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ResolveRequest => ResolveRequest.decode(value),
    responseSerialize: (value: ResolveFloatResponse): Buffer =>
      Buffer.from(ResolveFloatResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ResolveFloatResponse => ResolveFloatResponse.decode(value),
  },
  resolveObject: {
    path: "/featureflag.v1.Evaluation/ResolveObject" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ResolveRequest): Buffer => Buffer.from(ResolveRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ResolveRequest => ResolveRequest.decode(value),
    responseSerialize: (value: ResolveObjectResponse): Buffer =>
      Buffer.from(ResolveObjectResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ResolveObjectResponse => ResolveObjectResponse.decode(value),
  },
  resolveAll: {
    path: "/featureflag.v1.Evaluation/ResolveAll" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ResolveAllRequest): Buffer => Buffer.from(ResolveAllRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ResolveAllRequest => ResolveAllRequest.decode(value),
    responseSerialize: (value: ResolveAllResponse): Buffer => Buffer.from(ResolveAllResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ResolveAllResponse => ResolveAllResponse.decode(value),
  },
  streamEvents: {
    path: "/featureflag.v1.Evaluation/StreamEvents" as const,
    requestStream: false as const,
    responseStream: true as const,
    requestSerialize: (value: EventStreamRequest): Buffer => Buffer.from(EventStreamRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): EventStreamRequest => EventStreamRequest.decode(value),
    responseSerialize: (value: Event): Buffer => Buffer.from(Event.encode(value).finish()),
    responseDeserialize: (value: Buffer): Event => Event.decode(value),
  },
  /** Full flag + segment config at the current version, for a one-shot fetch. */
  getSnapshot: {
    path: "/featureflag.v1.Evaluation/GetSnapshot" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetSnapshotRequest): Buffer => Buffer.from(GetSnapshotRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetSnapshotRequest => GetSnapshotRequest.decode(value),
    responseSerialize: (value: SnapshotResponse): Buffer => Buffer.from(SnapshotResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SnapshotResponse => SnapshotResponse.decode(value),
  },
  /**
   * The current snapshot followed by a fresh snapshot on every config change, for
   * clients that evaluate locally in-process and stay live off this single stream.
   */
  streamSnapshot: {
    path: "/featureflag.v1.Evaluation/StreamSnapshot" as const,
    requestStream: false as const,
    responseStream: true as const,
    requestSerialize: (value: GetSnapshotRequest): Buffer => Buffer.from(GetSnapshotRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetSnapshotRequest => GetSnapshotRequest.decode(value),
    responseSerialize: (value: SnapshotResponse): Buffer => Buffer.from(SnapshotResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SnapshotResponse => SnapshotResponse.decode(value),
  },
} as const;

export interface EvaluationServer extends UntypedServiceImplementation {
  resolveBoolean: handleUnaryCall<ResolveRequest, ResolveBooleanResponse>;
  resolveString: handleUnaryCall<ResolveRequest, ResolveStringResponse>;
  resolveInteger: handleUnaryCall<ResolveRequest, ResolveIntegerResponse>;
  resolveFloat: handleUnaryCall<ResolveRequest, ResolveFloatResponse>;
  resolveObject: handleUnaryCall<ResolveRequest, ResolveObjectResponse>;
  resolveAll: handleUnaryCall<ResolveAllRequest, ResolveAllResponse>;
  streamEvents: handleServerStreamingCall<EventStreamRequest, Event>;
  /** Full flag + segment config at the current version, for a one-shot fetch. */
  getSnapshot: handleUnaryCall<GetSnapshotRequest, SnapshotResponse>;
  /**
   * The current snapshot followed by a fresh snapshot on every config change, for
   * clients that evaluate locally in-process and stay live off this single stream.
   */
  streamSnapshot: handleServerStreamingCall<GetSnapshotRequest, SnapshotResponse>;
}

export interface EvaluationClient extends Client {
  resolveBoolean(
    request: ResolveRequest,
    callback: (error: ServiceError | null, response: ResolveBooleanResponse) => void,
  ): ClientUnaryCall;
  resolveBoolean(
    request: ResolveRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ResolveBooleanResponse) => void,
  ): ClientUnaryCall;
  resolveBoolean(
    request: ResolveRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ResolveBooleanResponse) => void,
  ): ClientUnaryCall;
  resolveString(
    request: ResolveRequest,
    callback: (error: ServiceError | null, response: ResolveStringResponse) => void,
  ): ClientUnaryCall;
  resolveString(
    request: ResolveRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ResolveStringResponse) => void,
  ): ClientUnaryCall;
  resolveString(
    request: ResolveRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ResolveStringResponse) => void,
  ): ClientUnaryCall;
  resolveInteger(
    request: ResolveRequest,
    callback: (error: ServiceError | null, response: ResolveIntegerResponse) => void,
  ): ClientUnaryCall;
  resolveInteger(
    request: ResolveRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ResolveIntegerResponse) => void,
  ): ClientUnaryCall;
  resolveInteger(
    request: ResolveRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ResolveIntegerResponse) => void,
  ): ClientUnaryCall;
  resolveFloat(
    request: ResolveRequest,
    callback: (error: ServiceError | null, response: ResolveFloatResponse) => void,
  ): ClientUnaryCall;
  resolveFloat(
    request: ResolveRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ResolveFloatResponse) => void,
  ): ClientUnaryCall;
  resolveFloat(
    request: ResolveRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ResolveFloatResponse) => void,
  ): ClientUnaryCall;
  resolveObject(
    request: ResolveRequest,
    callback: (error: ServiceError | null, response: ResolveObjectResponse) => void,
  ): ClientUnaryCall;
  resolveObject(
    request: ResolveRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ResolveObjectResponse) => void,
  ): ClientUnaryCall;
  resolveObject(
    request: ResolveRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ResolveObjectResponse) => void,
  ): ClientUnaryCall;
  resolveAll(
    request: ResolveAllRequest,
    callback: (error: ServiceError | null, response: ResolveAllResponse) => void,
  ): ClientUnaryCall;
  resolveAll(
    request: ResolveAllRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ResolveAllResponse) => void,
  ): ClientUnaryCall;
  resolveAll(
    request: ResolveAllRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ResolveAllResponse) => void,
  ): ClientUnaryCall;
  streamEvents(request: EventStreamRequest, options?: Partial<CallOptions>): ClientReadableStream<Event>;
  streamEvents(
    request: EventStreamRequest,
    metadata?: Metadata,
    options?: Partial<CallOptions>,
  ): ClientReadableStream<Event>;
  /** Full flag + segment config at the current version, for a one-shot fetch. */
  getSnapshot(
    request: GetSnapshotRequest,
    callback: (error: ServiceError | null, response: SnapshotResponse) => void,
  ): ClientUnaryCall;
  getSnapshot(
    request: GetSnapshotRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SnapshotResponse) => void,
  ): ClientUnaryCall;
  getSnapshot(
    request: GetSnapshotRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SnapshotResponse) => void,
  ): ClientUnaryCall;
  /**
   * The current snapshot followed by a fresh snapshot on every config change, for
   * clients that evaluate locally in-process and stay live off this single stream.
   */
  streamSnapshot(request: GetSnapshotRequest, options?: Partial<CallOptions>): ClientReadableStream<SnapshotResponse>;
  streamSnapshot(
    request: GetSnapshotRequest,
    metadata?: Metadata,
    options?: Partial<CallOptions>,
  ): ClientReadableStream<SnapshotResponse>;
}

export const EvaluationClient = makeGenericClientConstructor(
  EvaluationService,
  "featureflag.v1.Evaluation",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): EvaluationClient;
  service: typeof EvaluationService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
