# yaml-language-server: $schema=./rule.schema.json
name: forward-push-to-argocd
when:
  all:
    - type: event
      kind: push
      source: github
    - any:
        - type: repository
          value: Accurate0/inf-k8s
        - type: repository
          value: Accurate0/home-gateway
actions:
  - run:
      - type: proxy_pass
        service: argocd
