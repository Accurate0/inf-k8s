apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: dawarich-managed-secrets
  namespace: dawarich
spec:
  refreshInterval: 900s
  secretStoreRef:
    name: infisical-dawarich-secret
    kind: SecretStore
  target:
    name: dawarich-managed-secrets
    creationPolicy: Owner
    template:
      type: Opaque
  dataFrom:
    - find:
        conversionStrategy: Default
        decodingStrategy: None
        name:
          regexp: ".*"
        nullBytePolicy: Ignore
