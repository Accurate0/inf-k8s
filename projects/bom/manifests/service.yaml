# yaml-language-server: $schema=https://k8s-schemas.anurag.sh/core/service_v1.json
apiVersion: v1
kind: Service
metadata:
  name: bom-api
  namespace: bom
spec:
  selector:
    app: bom
  ports:
    - name: api
      protocol: TCP
      port: 8000
      targetPort: 8000
