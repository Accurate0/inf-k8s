apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: janitor-bot-allow-all
  namespace: janitor-bot
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: janitor-bot-route
  authorization:
    defaultAction: Allow
