# yaml-language-server: $schema=https://k8s-schemas.anurag.sh/core/service_v1.json
apiVersion: v1
kind: Service
metadata:
  name: anurag-sh
  namespace: anurag-sh
spec:
  selector:
    app: anurag-sh
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
