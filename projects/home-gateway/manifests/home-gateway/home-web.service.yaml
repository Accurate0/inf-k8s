# yaml-language-server: $schema=https://k8s-schemas.anurag.sh/core/service_v1.json
apiVersion: v1
kind: Service
metadata:
  name: home-web
  namespace: home-gateway
  labels:
    app: home-web
spec:
  selector:
    app: home-web
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
